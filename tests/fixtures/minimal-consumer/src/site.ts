import { defineSiteConfig } from "albasimia-ssg-core/site-meta";

export const site = defineSiteConfig({
  name: "Consumer Site",
  siteUrl: "https://consumer.example/",
  description: "Consumer default description",
  locale: "en",
});
